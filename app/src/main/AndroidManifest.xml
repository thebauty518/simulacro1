<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.imaccimm24.simulacro1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".menuP" />
        <activity android:name=".que_visitar" />
        <activity android:name=".donde_dormir" />
        <activity android:name=".donde_comer" />
        <activity android:name=".que_hacer" />
        <activity android:name=".servicios_turisticos" />
        <activity android:name=".llamadas_urgentes" />
        <activity android:name=".Informacion"></activity>
    </application>

</manifest>